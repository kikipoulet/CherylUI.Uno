<Page
    x:Class="CherylUI.Uno.Demo.Pages.DialogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CherylUI.Uno.Demo.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Cheryl.Uno.Controls;assembly=Cheryl.Uno"
    mc:Ignorable="d"
    Background="Transparent">

    <controls:SliverPage Header="Dialogs">
        <StackPanel Spacing="65">
            <Grid>
            <Border Style="{StaticResource GlassBorderStyle}">
                <StackPanel Spacing="10">
                        <TextBlock FontFamily="{StaticResource QuicksandBold}" FontSize="16"   Text="Center Dialog"></TextBlock>
                        <TextBlock FontSize="14" Margin="0,0,0,18"  FontFamily="{StaticResource QuicksandRegular}" Foreground="{ThemeResource SecondaryText}" TextWrapping="Wrap" Text="Open a simple dialog at the center of the screen."></TextBlock>
                                </StackPanel>
            </Border>
                <Button Click="showdialog" Style="{StaticResource SmallButtonStyle}"  Margin="0,3,20,-21" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Open" ></Button>
                </Grid>

            <Grid>
                <Border Style="{StaticResource GlassBorderStyle}">
                    <StackPanel Spacing="10">
                        <TextBlock FontFamily="{StaticResource QuicksandBold}" FontSize="16"   Text="Bottom Sheet"></TextBlock>
                        <TextBlock FontSize="14" Margin="0,0,0,18"  FontFamily="{StaticResource QuicksandRegular}" Foreground="{ThemeResource SecondaryText}" TextWrapping="Wrap" Text="Open an animated bottom sheet. Disposable by clicking in the background."></TextBlock>
                    </StackPanel>
                </Border>
                <Button Click="showbottom" Style="{StaticResource SmallButtonStyle}"  Margin="0,3,20,-21" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Open" ></Button>
            </Grid>

        </StackPanel>
    </controls:SliverPage>
</Page>
